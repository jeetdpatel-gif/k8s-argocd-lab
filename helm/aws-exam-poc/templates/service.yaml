apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-exam-poc.fullname" . }}
  labels:
    {{- include "aws-exam-poc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-exam-poc.selectorLabels" . | nindent 4 }}
